# AI 生存游戏 MVP 开发计划

## Context
基于已锁定的 MVP-DESIGN.md，将设计转化为可执行的开发计划。游戏核心是"玩家每晚100字留言训练AI生存"，技术栈为纯Web Canvas + 本地LLM(Qwen3-14B) + Claude API。美术风格为简笔画纸张风（已有 sketch-animated.html 和 world.html 两个原型验证）。

## 关键参考文件
- `MVP-DESIGN.md` — 完整设计文档
- `sketch-animated.html` — 美术风格参考（手绘工具函数、动画系统）
- `world.html` — 大地图+相机+贝塞尔地形参考

---

## Phase 1：项目骨架 + 渲染引擎（3-4天）

### 1.1 项目结构搭建
```
ai-opw-survival/
├── index.html              # 入口
├── src/
│   ├── main.js             # 入口，初始化游戏
│   ├── game/
│   │   ├── GameLoop.js     # 主循环（白天tick/夜晚切换）
│   │   ├── GameState.js    # 全局状态管理（天数、阶段、AI状态）
│   │   ├── DayNight.js     # 昼夜周期控制（90秒白天+夜晚不限时）
│   │   └── Camera.js       # 相机跟随（从world.html迁移）
│   ├── world/
│   │   ├── MapGenerator.js # 地图生成（随机种子+贝塞尔地形）
│   │   ├── Terrain.js      # 地形数据和区域定义
│   │   ├── Resources.js    # 资源点管理（位置、数量、枯竭）
│   │   └── Objects.js      # 静态物件（树、石头、浆果丛等）
│   ├── entities/
│   │   ├── AICharacter.js  # AI角色（状态、背包、位置、表情）
│   │   ├── Animals.js      # 动物基类+各种动物行为
│   │   └── Combat.js       # 战斗系统
│   ├── systems/
│   │   ├── Inventory.js    # 背包+庇护所存储（8格/12格/20格）
│   │   ├── Crafting.js     # 合成系统（11配方+设施依赖）
│   │   ├── Hunger.js       # 饥饿系统
│   │   ├── Durability.js   # 耐久+腐烂
│   │   ├── Difficulty.js   # 难度递进+资源枯竭
│   │   └── Events.js       # 随机事件系统
│   ├── ai/
│   │   ├── LocalLLM.js     # Ollama API 调用封装
│   │   ├── CloudLLM.js     # Claude API 调用封装
│   │   ├── DecisionMaker.js# 白天决策逻辑（何时触发LLM）
│   │   ├── StrategyBook.js # 策略手册管理
│   │   └── EventLog.js     # 事件日志收集
│   ├── render/
│   │   ├── Renderer.js     # 主渲染器
│   │   ├── SketchTools.js  # 手绘工具（从sketch-animated.html迁移）
│   │   ├── Sprites.js      # 所有角色/物件绘制函数
│   │   ├── UI.js           # HUD（状态面板、天数、表情栏）
│   │   └── NightUI.js      # 夜晚界面（汇报、留言、策略变更）
│   └── data/
│       ├── recipes.js      # 合成配方定义
│       ├── creatures.js    # 生物属性定义
│       ├── terrains.js     # 地形属性定义
│       └── events.js       # 随机事件定义
└── assets/                 # 空，所有素材纯代码生成
```

### 1.2 渲染引擎迁移
从 sketch-animated.html 和 world.html 中提取并模块化：
- `SketchTools.js`：jitS()、wobble()、sketchLine()、sketchCircle()、sketchEllipse()、sketchArc()
- `Sprites.js`：所有绘制函数（drawTree、drawRock、drawAI、drawRabbit、drawWolf 等）
- `Renderer.js`：主渲染循环，背景缓存，深度排序
- `Camera.js`：平滑跟随

### 1.3 地图生成（两层分离）

**第一层：地形底图（静态缓存）**
- 从 world.html 的 drawBgToCache() 迁移，用贝塞尔曲线画地形色块
- 预定义一组基础贝塞尔控制点模板（和 world.html 里手写的类似）
- 随机种子只用于**偏移控制点坐标**（±50-150px），不改变整体拓扑
- 确保安全区（草地+花田）始终在出生点周围，区域从内到外递增难度
- 初始化时一次性画到 offscreen canvas 缓存，运行时直接 drawImage

**第二层：物件自动撒点（基于区域）**
- 地形底图画完后，建立区域归属查询（每个网格点属于哪个区域）
- 根据区域规则 + 随机种子自动生成物件：
  - 安全区：浆果丛(~20个)、草、止血草
  - 近区-森林：树、浆果丛(~40个)、蘑菇、溪边石头(~6个)、蛇隐藏点
  - 近区-沙地：粘土点、芦苇
  - 近区-水域：鱼
  - 中区-山地：石头(大量)、铁矿(3个)、熊
  - 中区-花田深处：蜂巢
  - 远区-沼泽：草药、枯树
  - 远区-雪地：少量资源
- 动物也按区域规则随机放置（兔子在安全区/近区，狼在中区/远区等）

### 1.4 验证标准
- 能看到随机生成的地图，AI角色在上面走动
- 相机平滑跟随
- 美术风格和 sketch-animated.html 一致

---

## Phase 2：核心游戏循环（3-4天）

### 2.1 游戏状态机
```
START → CHARACTER_SELECT → DAY_PHASE → NIGHT_SETTLE → NIGHT_REPORT → NIGHT_MESSAGE → STRATEGY_UPDATE → DAY_PHASE → ... → DEATH → RESULT
```

### 2.2 AI角色状态
```js
{
  hp: 100, maxHp: 100,
  hunger: 100, // 每秒递减
  position: {x, y},
  expression: 'normal',
  inventory: [], // 8格上限
  equipment: { weapon: null, armor: null },
  personality: 'cautious', // cautious | reckless | curious
  shelterPos: null,
  campfirePos: null,
  campfireFuel: 0,
}
```

### 2.3 昼夜循环
- 白天：90秒倒计时，AI自主行动
- 白天结束：一次性结算夜间后果（庇护所+篝火状态）
- 夜晚：显示结算结果 → AI汇报 → 玩家留言 → 策略变更展示 → 天亮

### 2.4 饥饿系统
- 每秒消耗饥饿值（速率待playtest）
- 饥饿=0时每秒掉血
- 进食恢复饥饿值（浆果少/烤肉多/蜂蜜最多）

### 2.5 耐久+腐烂
- 每个物品带 durability 或 expiryDay 字段
- 每天开始时检查并移除过期物品
- 工具使用后递减durability，为0时销毁

### 2.6 验证标准
- 完整的白天→夜晚→白天循环
- AI能移动、采集、吃东西
- 饥饿/生命值正常变化
- 物品会腐烂/损坏
- 夜晚能输入留言（此阶段留言不实际生效）

---

## Phase 3：世界交互（5-6天）

### 3.1 资源采集
- AI走到资源点旁边触发采集
- 不同资源采集时间不同（浆果快、砍树慢）
- 有石斧时采集效率翻倍
- 采集完成→物品入背包→资源点消耗（有限资源减少/无限资源不变）

### 3.2 合成系统
- 11个配方，检查背包材料+设施条件
- 设施依赖：陶罐/烤肉/火把需在篝火旁
- 合成结果入背包
- 铁矿全局只有3块，采完不刷新

### 3.3 建造系统
- 庇护所/木屋/篝火是放置型建筑
- 放置在AI当前位置
- 篝火每天消耗1木头（夜间结算时检查）
- 简易庇护所3天后自动销毁

### 3.4 动物AI
基础行为：
- 兔子/鹿：在区域内随机移动，AI靠近时逃跑
- 狐狸：随机移动，被攻击时反击
- 狼：朝AI移动并攻击（后期成群）
- 鱼：水边区域移动

复杂行为：
- 蛇：隐藏在草丛格子，AI踩到触发攻击+中毒
- 熊：守在铁矿点附近，AI靠近时怒吼（警告动画），再近攻击
- 乌鸦：随机事件触发，飞到AI旁边偷1-2个食物
- 蜜蜂：蜂巢区域，AI靠近被蛰，带火把靠近则驱散

### 3.5 战斗系统（简化）
- 攻击：AI对目标造成伤害（取决于武器）
- 逃跑：AI转身跑，成功率取决于速度差
- 受击：AI掉血，播放害怕表情
- 中毒：持续掉血（每秒少量），3天后自愈，草药立即解毒，止血草减缓

### 3.6 背包管理
- 8格上限，满时无法拾取
- 庇护所交互：靠近庇护所时可存取
- 物品堆叠（同类上限5）

### 3.7 验证标准
- 能采集所有类型资源
- 能合成所有11个配方
- 动物行为正确（追/逃/守护/偷）
- 战斗有结果
- 背包满时有反馈

---

## Phase 4：AI决策接入（5-6天）

### 4.1 本地LLM部署
- 安装 Ollama，下载 Qwen3-14B
- 封装 HTTP API 调用（localhost:11434）
- 测试响应格式和延迟

### 4.2 白天决策系统
决策触发点：
- 遇到新生物（视野范围内出现）
- 当前任务完成（采集完毕、到达目的地）
- 被攻击
- 饥饿值低于阈值
- 背包满

每次决策：
1. 收集当前环境状态（JSON）
2. 拼接 prompt（环境+策略手册+性格）
3. 调用 Qwen3-14B
4. 解析 JSON 响应 { action, thought }
5. 执行 action，显示 thought 为内心独白
6. 记录到事件日志

非决策时刻（本地规则）：
- 移动到目标点
- 正在采集
- 正在战斗
- 进食

### 4.3 AI默认行为（无策略手册时）
根据性格的初始行为：
- 谨慎型：留在安全区，只采浆果，遇到任何动物都跑
- 莽撞型：到处跑，看到东西就冲，不管危险
- 好奇型：往没去过的地方走，但不主动攻击

### 4.4 夜晚流程
1. 白天结束 → 夜间结算（庇护/寒冷/篝火燃料）
2. 调 Claude API 生成汇报（事件日志+性格→口语化汇报文本）
3. 显示汇报，等玩家输入留言
4. 玩家提交 → 调 Claude API 解析留言 → 返回新策略手册+变更摘要
5. 显示策略变更 diff
6. 玩家点"天亮了" → 进入下一天

### 4.5 策略手册管理
- 存储为纯文本（500-800 token）
- 每晚 Claude 更新时同时精简合并
- 传给 Qwen3 时作为 prompt 的一部分

### 4.6 事件日志
白天每次决策自动记录：
```json
{ "time": "12:30", "event": "遇到狼", "decision": "逃跑", "reason": "策略手册说血量低时逃跑", "state": { "hp": 45, "hunger": 60 } }
```

### 4.7 验证标准
- Qwen3能正确返回决策JSON
- AI行为符合策略手册
- 留言能改变第二天AI的行为
- 策略变更diff正确展示
- 内心独白有"性格"和"策略影响"的痕迹

---

## Phase 5：难度递进+事件系统（3-4天）

### 5.1 难度曲线
按天数触发：
- 第4天：狼开始成对出现
- 第7天：草地浆果枯竭、暴雨事件开始触发
- 第10天：森林浆果枯竭、乌鸦偷食加剧
- 第15天：暴风雪、狼群3只

### 5.2 资源枯竭
- 安全区浆果：总量固定（约20个），采完不刷新
- 森林浆果：总量固定（约40个），采完不刷新
- 森林溪边石头：约够2-3个石器
- 铁矿：全局3块
- 木头/草：无限

### 5.3 随机事件
- 每天白天有概率触发1个随机事件
- 事件池按天数解锁（早期只有乌鸦，后期加暴雨/暴风雪）
- 正面事件（废墟/温泉/蘑菇群）较少概率

### 5.4 验证标准
- 第7天后明显感受到资源压力
- 随机事件正确触发
- 难度递进让玩家不得不更新留言策略

---

## Phase 6：UI完善+结算（3-4天）

### 6.1 性格选择界面
- 三个AI角色并排展示（表情不同）
- 点击选择，显示性格描述
- 开始游戏

### 6.2 白天HUD优化
- 状态面板（生命/饥饿/背包）
- 天数+白天倒计时
- 内心独白气泡（关键决策时显示）
- 表情栏

### 6.3 夜晚界面
- 夜间结算展示
- AI汇报（带性格过滤）
- 留言输入（100字上限+字数计数）
- 策略变更diff展示
- "天亮了"按钮

### 6.4 结算/里程碑
- 死亡后展示生存报告
- 里程碑达成提示（5/10/20/30天）
- 训练回顾时间线（每天留言+AI变化）
- 再来一局按钮

### 6.5 验证标准
- 完整走完 选择性格→白天→夜晚→...→死亡→结算 全流程
- 所有UI元素和sketch-animated.html风格一致

---

## 总时间线

| Phase | 内容 | 预计时间 |
|-------|------|---------|
| 1 | 项目骨架+渲染引擎 | 3-4天 |
| 2 | 核心游戏循环 | 3-4天 |
| 3 | 世界交互 | 5-6天 |
| 4 | AI决策接入 | 5-6天 |
| 5 | 难度递进+事件 | 3-4天 |
| 6 | UI完善+结算 | 3-4天 |
| **总计** | | **22-28天（约4-6周）** |

每个Phase结束有明确的验证标准，通过后再进入下一阶段。

## 开发顺序的关键考量
- Phase 1-2 先出来就有东西可看可玩（AI在地图上跑+昼夜切换）
- Phase 3 加入交互后游戏可初步体验
- Phase 4 是核心差异化，AI真正"活"起来
- Phase 5-6 是体验打磨

## 技术风险点
1. **Qwen3-14B 输出不稳定**：可能返回非JSON格式，需要做容错处理+重试
2. **LLM决策质量**：策略手册的措辞直接影响决策质量，prompt需要反复调优
3. **90秒内事件密度**：地图大小和物件密度需要playtest反复调整
4. **随机种子地图**：贝塞尔曲线参数随机化后可能生成不合理地形，需要验证逻辑
